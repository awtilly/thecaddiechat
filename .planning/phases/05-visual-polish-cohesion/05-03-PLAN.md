---
phase: 05-visual-polish-cohesion
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Breadcrumb.astro
  - src/components/ArticleNav.astro
autonomous: true
requirements:
  - VISL-03
gap_closure: true

must_haves:
  truths:
    - "Zero hard-coded transition timing values remain anywhere in src/"
    - "Breadcrumb link hover transitions use motion token system"
    - "ArticleNav link hover transitions use motion token system"
  artifacts:
    - path: "src/components/Breadcrumb.astro"
      provides: "Token-based color transition on breadcrumb links"
      contains: "var(--transition-color)"
    - path: "src/components/ArticleNav.astro"
      provides: "Token-based color transition on article nav links"
      contains: "var(--transition-color)"
  key_links:
    - from: "src/components/Breadcrumb.astro"
      to: "src/styles/global.css"
      via: "CSS custom property reference"
      pattern: "var\\(--transition-color\\)"
    - from: "src/components/ArticleNav.astro"
      to: "src/styles/global.css"
      via: "CSS custom property reference"
      pattern: "var\\(--transition-color\\)"
---

<objective>
Replace the last 2 hard-coded transition values in the codebase with motion tokens, closing the single verification gap from Phase 5.

Purpose: VISL-03 requires consistent visual language across all pages. Breadcrumb.astro and ArticleNav.astro still use `transition: color 0.3s` instead of `var(--transition-color)`. This means they bypass the motion token system and do not respect `prefers-reduced-motion` overrides.
Output: Both files updated, zero hard-coded transition timing values remain in src/
</objective>

<execution_context>
@/Users/joestoehner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joestoehner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-visual-polish-cohesion/05-VERIFICATION.md

<interfaces>
<!-- Motion token from src/styles/global.css line 85 -->
<!-- Already defined and used by all other components in the codebase -->
```css
--transition-color: color var(--duration-fast) var(--ease-out);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hard-coded transitions in Breadcrumb and ArticleNav</name>
  <files>src/components/Breadcrumb.astro, src/components/ArticleNav.astro</files>
  <action>
    In `src/components/Breadcrumb.astro` line 36, replace:
      `transition: color 0.3s;`
    with:
      `transition: var(--transition-color);`

    In `src/components/ArticleNav.astro` line 42, replace:
      `transition: color 0.3s;`
    with:
      `transition: var(--transition-color);`

    These are single-line CSS property replacements. No other changes needed. The `--transition-color` token is already defined in global.css (line 85) and resolves to `color var(--duration-fast) var(--ease-out)`.
  </action>
  <verify>
    <automated>cd /Users/joestoehner/Desktop/GitHub/thecaddiechat && grep -rn "transition:.*[0-9]\+\(\.\|m\)s" src/ --include="*.astro" --include="*.css" | grep -v "node_modules" | grep -v "var(--" ; echo "EXIT: $?"</automated>
  </verify>
  <done>Both files use `var(--transition-color)` instead of hard-coded `color 0.3s`. The grep verification returns zero matches for hard-coded transition timing values in src/. Build passes with `npm run build`.</done>
</task>

</tasks>

<verification>
1. `grep -rn "transition:.*[0-9].*s" src/ --include="*.astro" --include="*.css" | grep -v "var(--"` returns zero results
2. `npm run build` succeeds
3. `npm run validate-urls` passes (all 19 URLs preserved)
</verification>

<success_criteria>
- Zero hard-coded transition timing values remain in src/ (Truth #10 from verification now passes)
- VISL-03 fully satisfied (was 98%, now 100%)
- Build and URL validation pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-visual-polish-cohesion/05-03-SUMMARY.md`
</output>
