---
phase: 05-visual-polish-cohesion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/mdx/InlineImage.astro
  - src/components/mdx/FullBleed.astro
  - src/components/mdx/ImagePair.astro
  - src/components/mdx/FeaturedImage.astro
  - src/components/mdx/Memorabilia.astro
  - src/pages/camera-roll/index.astro
  - src/pages/courses/index.astro
  - src/pages/courses/golden-age-golf/index.astro
  - src/pages/index.astro
autonomous: true
requirements: [VISL-03]

must_haves:
  truths:
    - "All 5 MDX image components use motion tokens instead of hard-coded transition values"
    - "All 4 page files use motion tokens instead of hard-coded transition values"
    - "Zero hard-coded transition timing values remain anywhere in src/"
    - "A visitor browsing homepage through courses to camera roll experiences identical animation timing"
  artifacts:
    - path: "src/components/mdx/InlineImage.astro"
      provides: "Token-based opacity transition"
      contains: "var(--duration-fast)"
    - path: "src/components/mdx/FullBleed.astro"
      provides: "Token-based image transform transition"
      contains: "var(--transition-image)"
    - path: "src/pages/index.astro"
      provides: "Token-based intro image transition"
      contains: "var(--transition-image)"
    - path: "src/pages/camera-roll/index.astro"
      provides: "Token-based grid image opacity transition"
      contains: "var(--duration-fast)"
  key_links:
    - from: "all MDX components"
      to: "src/styles/global.css"
      via: "CSS custom property references"
      pattern: "var\\(--duration-|var\\(--ease-|var\\(--transition-"
    - from: "all page files"
      to: "src/styles/global.css"
      via: "CSS custom property references"
      pattern: "var\\(--duration-|var\\(--ease-|var\\(--transition-"
---

<objective>
Migrate all 5 MDX image components and 4 page-level style blocks from hard-coded transition values to Phase 3 motion tokens, completing the codebase-wide consistency audit.

Purpose: Eliminate the remaining hard-coded transition values in MDX components and pages so that every animation in the entire codebase uses the same motion token system (VISL-03). This ensures a visitor experiences identical animation timing whether viewing a course article image, the camera roll gallery, or the homepage.
Output: 9 files migrated to motion tokens, zero hard-coded transition values remaining in src/.
</objective>

<execution_context>
@/Users/joestoehner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joestoehner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-visual-polish-cohesion/05-RESEARCH.md

<interfaces>
<!-- Motion tokens from global.css (defined in Phase 3) -- use these as replacements -->

From src/styles/global.css:
```css
/* Durations */
--duration-fast:    200ms;
--duration-normal:  350ms;
--duration-slow:    600ms;
--duration-slower:  900ms;

/* Easing */
--ease-out: cubic-bezier(0.22, 1, 0.36, 1);

/* Composed shorthands */
--transition-color:   color var(--duration-fast) var(--ease-out);
--transition-image:   transform var(--duration-slower) var(--ease-out);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 5 MDX image components to motion tokens</name>
  <files>
    src/components/mdx/InlineImage.astro,
    src/components/mdx/FullBleed.astro,
    src/components/mdx/ImagePair.astro,
    src/components/mdx/FeaturedImage.astro,
    src/components/mdx/Memorabilia.astro
  </files>
  <action>
Replace every hard-coded transition declaration with the corresponding motion token reference. Each component has 1-2 transition lines to change.

**InlineImage.astro:**
- Line ~27: `transition: opacity 0.3s ease` -> `transition: opacity var(--duration-fast) var(--ease-out)`

**FullBleed.astro:**
- Line ~32: `transition: transform 0.8s ease` -> `transition: var(--transition-image)`

**ImagePair.astro:**
- Line ~28: `transition: transform 0.6s ease, opacity 0.3s ease` -> `transition: transform var(--duration-slow) var(--ease-out), opacity var(--duration-fast) var(--ease-out)`

**FeaturedImage.astro:**
- Line ~32: `transition: opacity 0.3s ease` -> `transition: opacity var(--duration-fast) var(--ease-out)`

**Memorabilia.astro:**
- Line ~42: `transition: transform 0.4s ease, opacity 0.3s ease` -> `transition: transform var(--duration-fast) var(--ease-out), opacity var(--duration-fast) var(--ease-out)`

Each change is a single line replacement. Do not change any other CSS properties, hover effects, or HTML structure.
  </action>
  <verify>
    <automated>cd /Users/joestoehner/Desktop/GitHub/thecaddiechat && npm run build && grep -r "transition:.*[0-9].*ease" src/components/mdx/; echo "Exit: $?"</automated>
  </verify>
  <done>All 5 MDX image components use motion token references. Zero hard-coded transition values remain in src/components/mdx/. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate 4 page files to motion tokens and verify full codebase consistency</name>
  <files>
    src/pages/camera-roll/index.astro,
    src/pages/courses/index.astro,
    src/pages/courses/golden-age-golf/index.astro,
    src/pages/index.astro
  </files>
  <action>
Replace every hard-coded transition declaration in page-level style blocks with motion token references.

**camera-roll/index.astro:**
- Line ~91: `transition: opacity 0.3s ease` (grid images) -> `transition: opacity var(--duration-fast) var(--ease-out)`

**courses/index.astro:**
- Line ~82: `transition: opacity 0.5s ease` (dozer scene) -> `transition: opacity var(--duration-normal) var(--ease-out)`

**courses/golden-age-golf/index.astro:**
- Line ~92: `transition: color 0.3s ease` (section label link) -> `transition: var(--transition-color)`

**index.astro (homepage):**
- Line ~123: `transition: transform 0.8s ease` (intro image) -> `transition: var(--transition-image)`

After making all changes, run the full codebase grep to confirm zero hard-coded transition values remain anywhere in src/:
`grep -r "transition:.*[0-9].*ease" src/ --include="*.astro" --include="*.css"`

This should return zero matches, confirming complete token migration across the entire codebase.
  </action>
  <verify>
    <automated>cd /Users/joestoehner/Desktop/GitHub/thecaddiechat && npm run build && npm run validate-urls && grep -r "transition:.*[0-9].*ease" src/ --include="*.astro" --include="*.css"; echo "Grep exit: $?"</automated>
  </verify>
  <done>All 4 page files use motion token references. Full codebase grep for hard-coded transition values returns zero matches. Build succeeds and all 19 URLs validated.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. `npm run validate-urls` passes (all 19 URLs present)
3. `grep -r "transition:.*[0-9].*ease" src/ --include="*.astro" --include="*.css"` returns zero matches -- COMPLETE token migration
4. `grep -c "var(--" src/components/mdx/InlineImage.astro src/components/mdx/FullBleed.astro src/components/mdx/ImagePair.astro src/components/mdx/FeaturedImage.astro src/components/mdx/Memorabilia.astro` returns 1+ per file
</verification>

<success_criteria>
- All 5 MDX image components use motion tokens (zero hard-coded values)
- All 4 page files use motion tokens (zero hard-coded values)
- Full codebase grep confirms zero hard-coded transition timing values in any .astro or .css file
- Build passes and all 19 URLs preserved
- Animation timing is now guaranteed consistent across every page -- the same easing curves and durations apply everywhere
</success_criteria>

<output>
After completion, create `.planning/phases/05-visual-polish-cohesion/05-02-SUMMARY.md`
</output>
