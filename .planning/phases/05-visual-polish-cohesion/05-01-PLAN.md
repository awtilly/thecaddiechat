---
phase: 05-visual-polish-cohesion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ContentCard.astro
  - src/components/PostCard.astro
  - src/components/FeaturedPost.astro
  - src/components/CourseItem.astro
  - src/components/Footer.astro
  - src/components/Lightbox.astro
  - src/styles/global.css
  - src/layouts/ArticleLayout.astro
  - src/layouts/CourseLayout.astro
autonomous: true
requirements: [VISL-02, VISL-03]

must_haves:
  truths:
    - "All card components (ContentCard, PostCard, FeaturedPost, CourseItem) show cursor:pointer on hover"
    - "Footer links have animated underline on hover matching Nav pattern"
    - "All 6 UI components use motion tokens instead of hard-coded transition values"
    - "CourseItem hover uses background + border-left accent instead of padding shift"
    - "Keyboard users see visible focus rings on all interactive elements"
    - "Article body links in chronicles and courses have styled hover states"
  artifacts:
    - path: "src/styles/global.css"
      provides: "Global :focus-visible rule and :focus:not(:focus-visible) reset"
      contains: ":focus-visible"
    - path: "src/components/Footer.astro"
      provides: "Token-based transitions and animated underline hover"
      contains: "var(--transition-color)"
    - path: "src/components/CourseItem.astro"
      provides: "Token-based transitions without padding shift hover"
      contains: "var(--duration-fast)"
    - path: "src/layouts/ArticleLayout.astro"
      provides: "Article body link hover styles"
      contains: ".article-body a"
    - path: "src/layouts/CourseLayout.astro"
      provides: "Course body link hover styles"
      contains: ".course-body a"
  key_links:
    - from: "src/components/ContentCard.astro"
      to: "src/styles/global.css"
      via: "CSS custom property references"
      pattern: "var\\(--duration-fast\\)|var\\(--ease-out\\)|var\\(--transition-image\\)"
    - from: "src/components/Footer.astro"
      to: "src/components/Nav.astro"
      via: "Matching ::after underline animation pattern"
      pattern: "::after.*scaleX"
    - from: "src/styles/global.css"
      to: "all interactive elements"
      via: ":focus-visible global rule"
      pattern: ":focus-visible"
---

<objective>
Migrate all 6 UI component transition values to Phase 3 motion tokens, add cursor:pointer to card components, fix CourseItem layout-shifting hover, add Footer underline animation matching Nav pattern, add global focus-visible rule, and add article body link hover styles.

Purpose: Establish consistent animation timing across all UI components (VISL-03) and add the premium interaction patterns that are currently missing (VISL-02) -- focus-visible states, footer link underlines, article body link hovers.
Output: 6 polished components using the motion token system, global focus-visible accessibility rule, and styled in-content links.
</objective>

<execution_context>
@/Users/joestoehner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joestoehner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-visual-polish-cohesion/05-RESEARCH.md

<interfaces>
<!-- Motion tokens from global.css (defined in Phase 3) -- use these as replacements -->

From src/styles/global.css:
```css
/* Easing */
--ease-out:        cubic-bezier(0.22, 1, 0.36, 1);
--ease-in:         cubic-bezier(0.55, 0.055, 0.675, 0.19);
--ease-in-out:     cubic-bezier(0.65, 0, 0.35, 1);
--ease-out-expo:   cubic-bezier(0.16, 1, 0.3, 1);
--ease-out-back:   cubic-bezier(0.34, 1.56, 0.64, 1);

/* Durations */
--duration-instant: 100ms;
--duration-fast:    200ms;
--duration-normal:  350ms;
--duration-slow:    600ms;
--duration-slower:  900ms;

/* Composed shorthands */
--transition-color:   color var(--duration-fast) var(--ease-out);
--transition-hover:   all var(--duration-fast) var(--ease-out);
--transition-reveal:  opacity var(--duration-slow) var(--ease-out), transform var(--duration-slow) var(--ease-out);
--transition-image:   transform var(--duration-slower) var(--ease-out);

/* Color tokens */
--color-accent: #2d5a3f;
--color-accent-light: #3a7352;
--color-accent-dim: rgba(45, 90, 63, 0.08);
```

From src/components/Nav.astro (underline animation pattern to replicate on Footer):
```css
.nav-links a {
  position: relative;
  transition: var(--transition-color);
}
.nav-links a::after {
  content: '';
  position: absolute;
  bottom: -4px;
  left: 0;
  width: 100%;
  height: 1.5px;
  background: var(--color-accent);
  transform: scaleX(0);
  transform-origin: right;
  transition: transform var(--duration-normal) var(--ease-out);
}
.nav-links a:hover::after {
  transform: scaleX(1);
  transform-origin: left;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 6 UI components to motion tokens and fix hover patterns</name>
  <files>
    src/components/ContentCard.astro,
    src/components/PostCard.astro,
    src/components/FeaturedPost.astro,
    src/components/CourseItem.astro,
    src/components/Footer.astro,
    src/components/Lightbox.astro
  </files>
  <action>
Replace every hard-coded transition declaration with motion token references. Also add cursor:pointer to card-level elements and fix the CourseItem padding-shift hover.

**ContentCard.astro:**
- Line ~32: `transition: border-color 0.4s ease, transform 0.4s ease, box-shadow 0.4s ease` -> `transition: border-color var(--duration-fast) var(--ease-out), transform var(--duration-fast) var(--ease-out), box-shadow var(--duration-fast) var(--ease-out)`
- Add `cursor: pointer;` to `.content-card` rule
- Line ~51: `transition: transform 0.8s ease` (image) -> `transition: var(--transition-image)`

**PostCard.astro:**
- Line ~30: `transition: border-color 0.4s ease, transform 0.4s ease, box-shadow 0.4s ease` -> `transition: border-color var(--duration-fast) var(--ease-out), transform var(--duration-fast) var(--ease-out), box-shadow var(--duration-fast) var(--ease-out)`
- Add `cursor: pointer;` to `.post-card` rule
- Line ~48: `transition: transform 0.8s ease` (image) -> `transition: var(--transition-image)`

**FeaturedPost.astro:**
- Line ~44: `transition: transform 0.8s ease` (image) -> `transition: var(--transition-image)`
- Add `cursor: pointer;` to `.featured-post` rule (or the link wrapper)

**CourseItem.astro:**
- Line ~37: `transition: background 0.3s ease, padding 0.3s ease` -> `transition: background var(--duration-fast) var(--ease-out), border-left-color var(--duration-fast) var(--ease-out)`
- Add `cursor: pointer;` to `.course-item`
- Add `border-left: 3px solid transparent;` to `.course-item` base rule
- Change `.course-item:hover`: Remove `padding-left: var(--space-sm); padding-right: var(--space-sm);` and replace with `border-left-color: var(--color-accent);` -- this preserves the visual "indent" cue without layout reflow that interferes with Lenis smooth scroll
- Line ~71: `transition: transform 0.5s ease` (thumb image) -> `transition: var(--transition-image)`
- Line ~90: `transition: color 0.3s ease, transform 0.3s ease` (arrow) -> `transition: color var(--duration-fast) var(--ease-out), transform var(--duration-fast) var(--ease-out)`

**Footer.astro:**
- Line ~67: `transition: color 0.3s ease` -> `transition: var(--transition-color)`
- Add `position: relative;` to `.footer-links a` rule
- Add `.footer-links a::after` pseudo-element matching the Nav underline pattern (see interfaces block): `content: ''; position: absolute; bottom: -4px; left: 0; width: 100%; height: 1.5px; background: var(--color-accent); transform: scaleX(0); transform-origin: right; transition: transform var(--duration-normal) var(--ease-out);`
- Add `.footer-links a:hover::after { transform: scaleX(1); transform-origin: left; }`
- Exclude the StoekMedia link from the underline: add `.footer-links :global(.nav-stoek::after) { display: none; }` (matching how Nav handles it)
- Line ~96: `transition: color 0.3s` (social icon) -> `transition: var(--transition-color)`

**Lightbox.astro:**
- Line ~44: `transition: color 0.3s ease` (close button) -> `transition: var(--transition-color)`
- Line ~59: `transition: color 0.3s ease` (nav buttons) -> `transition: var(--transition-color)`

Do NOT use `transition: all` on card-level elements (ContentCard, PostCard) -- use specific properties to avoid interference with Astro View Transitions. The `--transition-hover` shorthand uses `all` which is fine for elements not participating in view transitions, but cards may be animated during page navigation.
  </action>
  <verify>
    <automated>cd /Users/joestoehner/Desktop/GitHub/thecaddiechat && npm run build && grep -r "transition:.*[0-9].*ease" src/components/ContentCard.astro src/components/PostCard.astro src/components/FeaturedPost.astro src/components/CourseItem.astro src/components/Footer.astro src/components/Lightbox.astro; echo "Exit: $?"</automated>
  </verify>
  <done>All 6 UI components use motion token references (zero hard-coded transition values). Cards have cursor:pointer. CourseItem hover uses border-left accent instead of padding shift. Footer links have animated underline matching Nav pattern. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Add global focus-visible rule and article body link styles</name>
  <files>
    src/styles/global.css,
    src/layouts/ArticleLayout.astro,
    src/layouts/CourseLayout.astro
  </files>
  <action>
**global.css -- Add focus-visible rules after the existing button styles (after line ~140):**

```css
/* Focus-visible: keyboard accessibility */
:focus-visible {
  outline: 2px solid var(--color-accent);
  outline-offset: 3px;
}
:focus:not(:focus-visible) {
  outline: none;
}
```

Do NOT add component-specific focus-visible rules (e.g., `.content-card:focus-visible`) -- the global rule with `outline-offset: 3px` is sufficient and consistent. Adding per-component overrides creates maintenance burden for no visual benefit.

**ArticleLayout.astro -- Add article body link styles in the `<style is:global>` block, after the existing `.article-body p` rule:**

```css
.article-body a {
  color: var(--color-accent);
  text-decoration: underline;
  text-underline-offset: 3px;
  text-decoration-color: rgba(45, 90, 63, 0.3);
  transition: color var(--duration-fast) var(--ease-out), text-decoration-color var(--duration-fast) var(--ease-out);
}
.article-body a:hover {
  color: var(--color-accent-light);
  text-decoration-color: var(--color-accent-light);
}
```

**CourseLayout.astro -- Add course body link styles in the `<style is:global>` block, after the existing `.course-body p` rule:**

```css
.course-body a {
  color: var(--color-accent);
  text-decoration: underline;
  text-underline-offset: 3px;
  text-decoration-color: rgba(45, 90, 63, 0.3);
  transition: color var(--duration-fast) var(--ease-out), text-decoration-color var(--duration-fast) var(--ease-out);
}
.course-body a:hover {
  color: var(--color-accent-light);
  text-decoration-color: var(--color-accent-light);
}
```

These link styles use motion tokens so they automatically respect prefers-reduced-motion (duration tokens zero out globally).
  </action>
  <verify>
    <automated>cd /Users/joestoehner/Desktop/GitHub/thecaddiechat && npm run build && grep -c "focus-visible" src/styles/global.css && grep -c "article-body a" src/layouts/ArticleLayout.astro && grep -c "course-body a" src/layouts/CourseLayout.astro</automated>
  </verify>
  <done>Global :focus-visible rule exists in global.css. Article body links in ArticleLayout and CourseLayout have accent-colored hover states with smooth transitions. Build succeeds. All new transitions use motion tokens (no hard-coded values).</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. `grep -r "transition:.*[0-9].*ease" src/components/ContentCard.astro src/components/PostCard.astro src/components/FeaturedPost.astro src/components/CourseItem.astro src/components/Footer.astro src/components/Lightbox.astro` returns zero matches
3. `grep -c "focus-visible" src/styles/global.css` returns 2+
4. `grep -c "cursor: pointer" src/components/ContentCard.astro src/components/PostCard.astro src/components/CourseItem.astro` returns 1+ per file
5. `grep "padding-left\|padding-right" src/components/CourseItem.astro` returns zero matches in hover rule
</verification>

<success_criteria>
- All 6 UI components use motion token references with zero hard-coded transition values
- Cards show pointer cursor on hover
- CourseItem hover does not shift layout (no padding change)
- Footer links have animated underline matching Nav pattern
- :focus-visible rule provides keyboard accessibility globally
- Article body links in both ArticleLayout and CourseLayout have styled hover states
- Build passes, all 19 URLs preserved
</success_criteria>

<output>
After completion, create `.planning/phases/05-visual-polish-cohesion/05-01-SUMMARY.md`
</output>
