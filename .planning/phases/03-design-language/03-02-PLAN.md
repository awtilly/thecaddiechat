---
phase: 03-design-language
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/layouts/ArticleLayout.astro
  - src/layouts/CourseLayout.astro
  - src/layouts/PageLayout.astro
  - src/components/Nav.astro
autonomous: false
requirements:
  - VISL-01
  - ANIM-04

must_haves:
  truths:
    - "A chronicle story page reads like a magazine feature with drop cap, refined pull quotes, and clear typographic hierarchy"
    - "Course pages have consistent typographic quality matching the chronicles"
    - "Page titles use the fluid type scale tokens"
    - "Navigation transitions use motion tokens instead of magic values"
    - "All existing pages still render correctly with no broken layouts"
  artifacts:
    - path: "src/layouts/ArticleLayout.astro"
      provides: "Magazine-quality article typography with drop cap, tokenized font sizes, pull quote styling, motion-tokenized transitions"
      contains: "--font-size-"
    - path: "src/layouts/CourseLayout.astro"
      provides: "Tokenized course typography with refined drop cap, consistent with article styling"
      contains: "--font-size-"
    - path: "src/layouts/PageLayout.astro"
      provides: "Tokenized page title typography and motion-tokenized animation"
      contains: "--font-size-"
    - path: "src/components/Nav.astro"
      provides: "Motion-tokenized navigation transitions"
      contains: "--duration-"
  key_links:
    - from: "src/layouts/ArticleLayout.astro"
      to: "src/styles/global.css"
      via: "CSS custom property references"
      pattern: "var\\(--font-size-|var\\(--leading-|var\\(--tracking-|var\\(--measure-|var\\(--ease-|var\\(--duration-"
    - from: "src/layouts/CourseLayout.astro"
      to: "src/styles/global.css"
      via: "CSS custom property references"
      pattern: "var\\(--font-size-|var\\(--leading-"
    - from: "src/components/Nav.astro"
      to: "src/styles/global.css"
      via: "CSS custom property references"
      pattern: "var\\(--duration-|var\\(--ease-"
---

<objective>
Apply the typography and motion tokens from Plan 01 to the three layout files and navigation component, transforming chronicles into magazine-quality reading experiences and establishing consistent tokenized styling across layouts.

Purpose: Realize the design language by replacing ad-hoc font sizes and transitions with systematic tokens. The chronicle ArticleLayout is the primary transformation target -- it should read like a magazine feature.
Output: ArticleLayout with drop cap, tokenized typography hierarchy, refined pull quotes. CourseLayout with matching quality. PageLayout with tokenized page titles. Nav with motion tokens.
</objective>

<execution_context>
@/Users/joestoehner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joestoehner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-design-language/03-RESEARCH.md
@.planning/phases/03-design-language/03-01-SUMMARY.md

<interfaces>
<!-- Tokens defined in Plan 01 that this plan consumes. -->

From src/styles/global.css :root (added by Plan 01):
```css
/* Typography Scale */
--font-size-xs, --font-size-sm, --font-size-base, --font-size-md,
--font-size-lg, --font-size-xl, --font-size-2xl, --font-size-3xl, --font-size-4xl

/* Line Heights */
--leading-tight, --leading-snug, --leading-normal, --leading-relaxed, --leading-loose

/* Tracking */
--tracking-tight, --tracking-normal, --tracking-wide, --tracking-wider, --tracking-widest

/* Measure */
--measure-narrow, --measure-normal, --measure-wide

/* Motion Easings */
--ease-out, --ease-in, --ease-in-out, --ease-out-expo, --ease-out-back

/* Motion Durations */
--duration-instant, --duration-fast, --duration-normal, --duration-slow, --duration-slower

/* Motion Distances */
--distance-sm, --distance-md, --distance-lg

/* Transition Shorthands */
--transition-color, --transition-hover, --transition-reveal, --transition-image
```

Existing variables (unchanged):
```css
--font-display, --font-body, --color-accent, --color-text-primary,
--color-text-secondary, --color-text-muted, --color-border, --color-border-strong,
--space-xs, --space-sm, --space-md, --space-lg, --space-xl, --space-2xl,
--nav-height, --max-width
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Transform ArticleLayout into magazine-quality reading experience</name>
  <files>src/layouts/ArticleLayout.astro</files>
  <action>
Replace ad-hoc font sizes, line heights, and transitions in ArticleLayout.astro with token references. Add drop cap styling. Enhance pull quotes with editorial borders.

**Scoped styles (inside `<style>` block):**

`.breadcrumb` font-size: change `0.75rem` to `var(--font-size-xs)`
`.breadcrumb` letter-spacing: change `0.1em` to `var(--tracking-wide)`
`.breadcrumb a` transition: change `color 0.3s` to `var(--transition-color)`

`.article-header h1` font-size: keep the clamp() as-is since it already matches --font-size-2xl range. REPLACE with `var(--font-size-2xl)` for consistency.
`.article-header h1` line-height: change `1.1` to `var(--leading-tight)`
`.article-header h1` letter-spacing: ADD `letter-spacing: var(--tracking-tight)`

`.article-meta` font-size: change `0.85rem` to `var(--font-size-sm)`

**Global styles (inside `<style is:global>` block):**

`.article-body` max-width: change `720px` to `var(--measure-wide)` (75ch is approximately 720px at article body font size)

`.article-body p`:
- font-size: change `1.05rem` to `var(--font-size-base)`
- line-height: change `1.9` to `var(--leading-loose)`

`.article-body h2`:
- font-size: change `1.8rem` to `var(--font-size-xl)`
- line-height: ADD `line-height: var(--leading-tight)`
- letter-spacing: ADD `letter-spacing: var(--tracking-tight)`

`.article-body h3`:
- font-size: change `1.3rem` to `var(--font-size-lg)`
- line-height: ADD `line-height: var(--leading-snug)`

`.article-body blockquote`:
- font-size: change `1.15rem` to `var(--font-size-md)`
- line-height: change `1.7` to `var(--leading-relaxed)`

`.article-body .pull-quote`:
- font-size: change `1.6rem` to `var(--font-size-xl)`
- line-height: change `1.4` to `var(--leading-snug)`
- padding: change `var(--space-lg) 0` to `var(--space-lg) var(--space-md)`
- margin: change `0 auto` to `var(--space-lg) auto`
- ADD `border-top: 1.5px solid var(--color-border-strong)`
- ADD `border-bottom: 1.5px solid var(--color-border-strong)`
- max-width: change `600px` to `none` (let it break out of article measure for visual impact)

`.article-inline-image figcaption`:
- font-size: change `0.8rem` to `var(--font-size-sm)`

**ADD new drop cap rule** (after `.article-body p` rule):
```css
.article-body > p:first-child::first-letter {
  font-family: var(--font-display);
  font-size: var(--font-size-4xl);
  float: left;
  line-height: 0.8;
  margin-right: 0.08em;
  margin-top: 0.05em;
  color: var(--color-accent);
  font-weight: 700;
}
```

This adapts the existing CourseLayout drop cap pattern (which uses 3.5rem/line-height:1) but uses the token and fine-tunes the line-height for Playfair Display at the larger 4xl size.
  </action>
  <verify>
    <automated>cd /Users/joestoehner/Desktop/GitHub/thecaddiechat && npm run build</automated>
  </verify>
  <done>ArticleLayout uses typography tokens throughout (no remaining magic font-size values except potentially responsive overrides). Drop cap renders on first paragraph. Pull quotes have editorial border-top/border-bottom styling. Blockquotes use token sizes. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Tokenize CourseLayout, PageLayout, and Nav</name>
  <files>src/layouts/CourseLayout.astro, src/layouts/PageLayout.astro, src/components/Nav.astro</files>
  <action>
Apply typography and motion tokens to the remaining core layout files and navigation.

**CourseLayout.astro -- scoped styles:**

`.course-number` font-size: keep `8rem` as-is (decorative, intentionally outside scale)
`.course-hero h1` font-size: REPLACE `clamp(2.5rem, 6vw, 4.5rem)` with `var(--font-size-3xl)` (range 49-61px is close to 2.5-4.5rem)
`.course-hero h1` line-height: change `1.1` to `var(--leading-tight)`
`.course-meta-label` font-size: change `0.7rem` to `var(--font-size-xs)`
`.course-meta-label` letter-spacing: change `0.15em` to `var(--tracking-wider)`
`.course-meta-value` font-size: change `1.1rem` to `var(--font-size-base)`
`.course-hero :global(.section-label a)` transition: change `color 0.3s` to `var(--transition-color)`

**CourseLayout.astro -- global styles:**

`.course-body p`:
- font-size: change `1.05rem` to `var(--font-size-base)`
- line-height: change `1.9` to `var(--leading-loose)`

`.course-body > p:first-child::first-letter`:
- font-size: change `3.5rem` to `var(--font-size-4xl)`
- line-height: change `1` to `0.8` (fine-tune for larger token size)
- margin-right: change `0.5rem` to `0.08em` (relative to letter size)
- margin-top: change `0.1rem` to `0.05em`
- ADD `font-weight: 700`

`.course-body h2`:
- font-size: change `1.6rem` to `var(--font-size-lg)`
- ADD `line-height: var(--leading-snug)`
- ADD `letter-spacing: var(--tracking-tight)`

`.course-body .pull-quote`:
- font-size: change `1.5rem` to `var(--font-size-xl)`
- line-height: change `1.5` to `var(--leading-snug)`
- border-top: change `1px solid var(--color-border)` to `1.5px solid var(--color-border-strong)` (match ArticleLayout)
- border-bottom: change `1px solid var(--color-border)` to `1.5px solid var(--color-border-strong)`

**PageLayout.astro -- scoped styles:**

`.page-header :global(h1)`:
- font-size: REPLACE `clamp(3rem, 7vw, 5.5rem)` with `var(--font-size-3xl)` (similar range, more harmonious)
- line-height: change `1.05` to `var(--leading-tight)`
- animation: change `fadeInUp 1s ease` to `fadeInUp var(--duration-slower) var(--ease-out)`

`.page-header p`:
- font-size: change `1.1rem` to `var(--font-size-base)`
- line-height: change `1.8` to `var(--leading-relaxed)`

**Nav.astro -- motion tokens only (typography stays as-is since nav sizes are intentionally specific):**

Replace ALL transition values in Nav.astro with motion tokens:
- `transition: all 0.4s ease` (line ~35, nav background) -> `transition: all var(--duration-normal) var(--ease-out)`
- `transition: color 0.3s ease` (line ~71, nav links) -> `transition: var(--transition-color)`
- `transition: transform 0.4s ease` (line ~84, hover underline) -> `transition: transform var(--duration-normal) var(--ease-out)`
- `transition: all 0.3s ease` (line ~121, mobile menu items) -> `transition: all var(--duration-fast) var(--ease-out)`
- `transition: opacity 0.3s ease` (line ~170, mobile overlay) -> `transition: opacity var(--duration-fast) var(--ease-out)`
- `transition: transform 0.4s ease` (line ~198, hamburger) -> `transition: transform var(--duration-normal) var(--ease-out)`
  </action>
  <verify>
    <automated>cd /Users/joestoehner/Desktop/GitHub/thecaddiechat && npm run build && npm run validate-urls</automated>
  </verify>
  <done>CourseLayout uses typography tokens with refined drop cap matching ArticleLayout quality. PageLayout page titles use type scale tokens with motion-tokenized animation. Nav uses motion tokens for all transitions. All 19 URLs validate. No layout regressions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of magazine typography and motion tokens</name>
  <action>
Human verifies the visual quality of the design language applied across all layouts.

What was built: Magazine-quality typography system and motion design tokens applied across all layouts. Key visual changes: (1) Drop cap on chronicle article opening paragraphs, (2) Refined pull quote borders, (3) Fluid type scale creating clear hierarchy, (4) True italic Playfair Display throughout, (5) Consistent editorial feel between chronicles and courses.

How to verify:
1. Run `npm run dev` and open http://localhost:4321/
2. Visit a chronicle page (e.g., /chronicles/viktor-hovland/) -- verify:
   - First paragraph has a large decorative drop cap letter in green
   - Body text has comfortable reading width and line spacing
   - h2/h3 headings show clear visual hierarchy
   - Pull quotes (if present) have top/bottom border lines and centered italic text
   - Italic Playfair text looks properly designed (not just slanted)
3. Visit a course page (e.g., /courses/stonewall/) -- verify:
   - Drop cap matches chronicle quality
   - Same comfortable reading experience
   - Course hero title is well-proportioned
4. Visit /chronicles/ or /courses/ listing page -- verify page title sizing feels right
5. Test navigation: hover over nav links -- transitions should feel smooth and intentional
6. (Optional) In browser devtools, toggle "prefers-reduced-motion: reduce" -- all animations should stop

Resume signal: Type "approved" or describe any visual issues to fix.
  </action>
  <verify>Human approval of visual quality</verify>
  <done>User confirms magazine-quality typography, drop caps render correctly, pull quotes look editorial, motion feels intentional, and no visual regressions across all page types.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. `npm run validate-urls` confirms all 19 URLs
3. ArticleLayout.astro uses --font-size-*, --leading-*, --tracking-* tokens (no magic font-size values)
4. ArticleLayout has ::first-letter drop cap rule
5. CourseLayout typography matches ArticleLayout quality and uses same token system
6. PageLayout h1 uses type scale token
7. Nav transitions use --duration-* and --ease-* tokens
8. Human verifies visual quality of typography and drop caps
</verification>

<success_criteria>
- Chronicle pages read like magazine features: drop cap, clear hierarchy, refined pull quotes, comfortable line length
- Course pages match the editorial quality of chronicles
- Page titles use the fluid type scale
- Navigation feels smooth with intentional motion timing
- All transitions in layouts and nav use token references (no magic 0.3s ease values)
- Human approves visual quality at checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/03-design-language/03-02-SUMMARY.md`
</output>
