---
phase: 03-design-language
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
  - src/layouts/BaseLayout.astro
autonomous: true
requirements:
  - VISL-01
  - ANIM-04

must_haves:
  truths:
    - "Typography tokens exist as CSS custom properties on :root and are importable by any component"
    - "Motion tokens (easing, duration, distance) exist as CSS custom properties on :root"
    - "Playfair Display true italic renders instead of faux italic"
    - "prefers-reduced-motion zeroes all motion tokens globally"
    - "Existing site appearance is unchanged -- tokens are defined but not yet applied to layouts"
  artifacts:
    - path: "src/styles/global.css"
      provides: "Typography scale tokens, line-height tokens, tracking tokens, measure tokens, motion tokens, transition shorthands, reduced-motion reset"
      contains: "--font-size-base"
    - path: "src/layouts/BaseLayout.astro"
      provides: "Playfair Display italic import"
      contains: "wght-italic.css"
  key_links:
    - from: "src/styles/global.css"
      to: ":root"
      via: "CSS custom properties"
      pattern: "--font-size-|--ease-|--duration-|--distance-|--leading-|--tracking-|--measure-"
    - from: "src/layouts/BaseLayout.astro"
      to: "@fontsource-variable/playfair-display"
      via: "import statement"
      pattern: "wght-italic"
---

<objective>
Define the complete typography and motion design token system as CSS custom properties in global.css, and import the Playfair Display true italic variant.

Purpose: Establish the design language foundation that all future styling and animation work builds upon. Tokens are defined in this plan; applied to layouts in Plan 02.
Output: Typography scale (--font-size-*), line-height (--leading-*), tracking (--tracking-*), measure (--measure-*), easing (--ease-*), duration (--duration-*), distance (--distance-*), transition shorthands (--transition-*), and prefers-reduced-motion reset. True italic font loaded.
</objective>

<execution_context>
@/Users/joestoehner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joestoehner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-design-language/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define typography and motion tokens in global.css</name>
  <files>src/styles/global.css</files>
  <action>
Add new CSS custom properties to the EXISTING :root block in src/styles/global.css. Do NOT rename or remove any existing variables (--color-*, --font-display, --font-body, --space-*, --max-width, --nav-height). ADD these new token groups after the existing variables:

**Typography Scale (fluid clamp, Major Third 1.25 ratio, 320px-1280px viewport):**
```
--font-size-xs:    clamp(0.64rem, 0.59rem + 0.24vw, 0.80rem);   /* 10-13px: meta labels */
--font-size-sm:    clamp(0.80rem, 0.74rem + 0.30vw, 1.00rem);   /* 13-16px: captions */
--font-size-base:  clamp(1.00rem, 0.93rem + 0.38vw, 1.25rem);   /* 16-20px: body text */
--font-size-md:    clamp(1.25rem, 1.16rem + 0.47vw, 1.56rem);   /* 20-25px: large body */
--font-size-lg:    clamp(1.56rem, 1.45rem + 0.59vw, 1.95rem);   /* 25-31px: h3 */
--font-size-xl:    clamp(1.95rem, 1.81rem + 0.73vw, 2.44rem);   /* 31-39px: h2 */
--font-size-2xl:   clamp(2.44rem, 2.27rem + 0.92vw, 3.05rem);   /* 39-49px: h1 article */
--font-size-3xl:   clamp(3.05rem, 2.83rem + 1.15vw, 3.81rem);   /* 49-61px: page titles */
--font-size-4xl:   clamp(3.81rem, 3.54rem + 1.44vw, 4.77rem);   /* 61-76px: hero */
```

**Line Heights:**
```
--leading-tight:   1.1;    /* display headings */
--leading-snug:    1.25;   /* subheadings */
--leading-normal:  1.5;    /* UI text */
--leading-relaxed: 1.7;    /* body copy */
--leading-loose:   1.9;    /* long-form reading (articles) */
```

**Tracking (letter-spacing):**
```
--tracking-tight:  -0.02em;  /* large display type */
--tracking-normal: 0;        /* body text */
--tracking-wide:   0.1em;    /* buttons, labels */
--tracking-wider:  0.15em;   /* small caps, meta */
--tracking-widest: 0.25em;   /* kickers, section labels */
```

**Measure (max line width):**
```
--measure-narrow:  45ch;   /* captions, sidebars */
--measure-normal:  65ch;   /* body text optimal */
--measure-wide:    75ch;   /* body text max */
```

**Motion -- Easing Curves (GSAP-compatible cubic-bezier values for Phase 4):**
```
--ease-out:        cubic-bezier(0.22, 1, 0.36, 1);         /* power2.out -- primary deceleration */
--ease-in:         cubic-bezier(0.55, 0.055, 0.675, 0.19);  /* power2.in -- acceleration */
--ease-in-out:     cubic-bezier(0.65, 0, 0.35, 1);         /* power2.inOut -- symmetric */
--ease-out-expo:   cubic-bezier(0.16, 1, 0.3, 1);          /* expo.out -- cinematic deceleration */
--ease-out-back:   cubic-bezier(0.34, 1.56, 0.64, 1);      /* slight overshoot for playful elements */
```

**Motion -- Durations:**
```
--duration-instant: 100ms;   /* micro-feedback: focus rings, color changes */
--duration-fast:    200ms;   /* hover states, small transitions */
--duration-normal:  350ms;   /* element transitions, reveals */
--duration-slow:    600ms;   /* large element transitions, image reveals */
--duration-slower:  900ms;   /* entrance animations, scroll reveals */
```

**Motion -- Distances (translateY values for reveal animations):**
```
--distance-sm:     12px;    /* subtle micro-movements */
--distance-md:     24px;    /* standard reveal offset */
--distance-lg:     40px;    /* dramatic entrance */
```

**Motion -- Composed Transition Shorthands:**
```
--transition-color:   color var(--duration-fast) var(--ease-out);
--transition-hover:   all var(--duration-fast) var(--ease-out);
--transition-reveal:  opacity var(--duration-slow) var(--ease-out), transform var(--duration-slow) var(--ease-out);
--transition-image:   transform var(--duration-slower) var(--ease-out);
```

**After the :root block, add the reduced-motion reset** (place just before the existing `*, *::before, *::after` reset line):
```css
@media (prefers-reduced-motion: reduce) {
  :root {
    --duration-instant: 0ms;
    --duration-fast:    0ms;
    --duration-normal:  0ms;
    --duration-slow:    0ms;
    --duration-slower:  0ms;
    --distance-sm:     0px;
    --distance-md:     0px;
    --distance-lg:     0px;
  }
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
  }
}
```

**Then apply motion tokens to the core global patterns already in global.css** (these are in global.css itself, not in component files):
- `.reveal` transition: change `opacity 0.8s ease, transform 0.8s ease` to `var(--transition-reveal)`
- `.reveal` transform: change `translateY(40px)` to `translateY(var(--distance-lg))`
- `@keyframes fadeInUp`: change `translateY(40px)` to `translateY(var(--distance-lg))`
- `.btn` transition: change `all 0.4s ease` to `var(--transition-hover)`
- `.btn-arrow::after` transition: change `transform 0.3s ease` to `transform var(--duration-fast) var(--ease-out)`

Do NOT touch any other component files in this task. Only modify global.css.
  </action>
  <verify>
    <automated>cd /Users/joestoehner/Desktop/GitHub/thecaddiechat && npm run build</automated>
  </verify>
  <done>global.css :root contains all typography tokens (--font-size-xs through --font-size-4xl, --leading-*, --tracking-*, --measure-*) and all motion tokens (--ease-*, --duration-*, --distance-*, --transition-*). prefers-reduced-motion media query zeroes durations and distances. Core global patterns (.reveal, .btn, .btn-arrow) use token references instead of magic values. All existing variables unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Import Playfair Display true italic variant</name>
  <files>src/layouts/BaseLayout.astro</files>
  <action>
In src/layouts/BaseLayout.astro frontmatter, add the Playfair Display italic import AFTER the existing upright import. The file currently has:
```
import '@fontsource-variable/playfair-display';
import '@fontsource-variable/dm-sans';
```

Change to:
```
import '@fontsource-variable/playfair-display';
import '@fontsource-variable/playfair-display/wght-italic.css';
import '@fontsource-variable/dm-sans';
```

This loads the true italic variant with redesigned letterforms (different 'a', 'e', 'g' shapes) instead of the browser's faux italic (which merely slants the upright glyphs). The site uses font-style: italic on Playfair Display in 18+ places across layouts and components.
  </action>
  <verify>
    <automated>cd /Users/joestoehner/Desktop/GitHub/thecaddiechat && npm run build && npm run validate-urls</automated>
  </verify>
  <done>BaseLayout imports wght-italic.css for Playfair Display. Build succeeds. All 19 URLs validate. Every font-style: italic on Playfair Display now renders true italic glyphs instead of faux italic.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds without CSS syntax errors
2. `npm run validate-urls` confirms all 19 URLs still valid
3. global.css :root block contains all --font-size-*, --leading-*, --tracking-*, --measure-*, --ease-*, --duration-*, --distance-*, --transition-* tokens
4. prefers-reduced-motion media query exists and zeroes all duration/distance tokens
5. BaseLayout.astro imports wght-italic.css
6. No existing CSS custom properties were modified or removed
</verification>

<success_criteria>
- Typography token system (9 font sizes, 5 line heights, 5 trackings, 3 measures) defined as CSS custom properties on :root
- Motion token system (5 easings, 5 durations, 3 distances, 4 transition shorthands) defined as CSS custom properties on :root
- prefers-reduced-motion resets all motion tokens to zero
- Global patterns (.reveal, .btn) use token references
- Playfair Display true italic loaded via wght-italic.css import
- Site builds and all URLs validate -- no visual regressions from token definitions alone
</success_criteria>

<output>
After completion, create `.planning/phases/03-design-language/03-01-SUMMARY.md`
</output>
